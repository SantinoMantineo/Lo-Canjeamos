const initialState = {
    // Tu estado inicial aquí
  };
  
  const reducer = (state = initialState, ) => {
    // Lógica de la función reductora aquí
    return state; // Este es un ejemplo, deberías modificarlo según tus necesidades
  };
  
  export default reducer;